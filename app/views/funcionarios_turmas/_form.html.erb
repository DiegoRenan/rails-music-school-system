<div class="panel panel-primary">
	<div class="panel-body">
		<%= form_for(@funcionarios_turma) do |f| %>
		<% if @funcionarios_turma.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@funcionarios_turma.errors.count, "error") %> prohibited this funcionarios_turma from being saved:</h2>

			<ul>
				<% @funcionarios_turma.errors.full_messages.each do |message| %>
				<li>
					<%= message %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
		<% s_array = Funcionario.all.map { |s| [s.nome, s.id] } %>
		<% t_array = Turma.all.map { |t| [t.nome, t.id] } %>
		<div class="form-group">
			<div class="field">
				<%= f.label :funcionarios_id %>
				<br>
				<% c_array = Funcionario.all.map { |c| [c.nome, c.id] } %>
				<%= f.select(:funcionario_id, options_for_select(c_array),{prompt: 'Selecione o professor: '}, {class: "form-control"} ) %>

			</div>
		</div>
		<div class="form-group">
			<div class="field">
				<%= f.label :turmas_id %>
				<br>
				<% c_array = Turma.all.map { |c| [c.nome, c.id] } %>
				<%= f.select(:turma_id, options_for_select(c_array),{prompt: 'Selecione a Turma: '}, {class: "form-control"} ) %>

			</div>
		</div>
		<div class="form-group">
			<div class="field">
				<%= f.label :status %>
				<br>
				<%= f.text_field :status,  class: "form-control"  %>
			</div>
		</div>

		<div class="actions">
			<%= f.submit %>
		</div>
		<% end %>
	</div>
</div>
